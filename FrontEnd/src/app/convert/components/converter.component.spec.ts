import { Converter.Component } from './converter.component';

describe('Converter.Component', () => {
  it('should create an instance', () => {
    expect(new Converter.Component()).toBeTruthy();
  });
});
